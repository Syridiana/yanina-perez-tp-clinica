import { SeasonsDirective } from './seasons.directive';

describe('SeasonsDirective', () => {
  it('should create an instance', () => {
    const directive = new SeasonsDirective();
    expect(directive).toBeTruthy();
  });
});
