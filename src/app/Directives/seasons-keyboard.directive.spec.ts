import { SeasonsKeyboardDirective } from './seasons-keyboard.directive';

describe('SeasonsKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new SeasonsKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
