import { SeasonsHoverDirective } from './seasons-hover.directive';

describe('SeasonsHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SeasonsHoverDirective();
    expect(directive).toBeTruthy();
  });
});
